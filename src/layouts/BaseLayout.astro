---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import { SITE_NAME, SITE_DESCRIPTION, SUPPORT_EMAIL } from '../consts';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  ogTitle?: string;
  ogDescription?: string;
  /**
   * Per-page OG / Twitter card image. Falls back to the site-wide
   * og-image.png. Blog post pages pass their cover image so social
   * shares pick up a per-article preview instead of the generic one.
   */
  ogImage?: string;
  ogType?: string;
  twitterCard?: string;
  canonicalPath?: string;
  currentPage?: string;
  /**
   * Optional per-page JSON-LD blob. The layout always emits the
   * site-wide WebSite + Organization graphs; pass `pageSchema` to
   * append page-specific schema (SoftwareApplication for pricing,
   * Article for blog posts, etc.) without re-declaring the root.
   */
  pageSchema?: unknown;
  /**
   * Force noindex regardless of PUBLIC_ALLOW_INDEXING. Use for pages
   * that should never be indexed even in prod (search results,
   * thin/dynamic query-driven pages).
   */
  noindex?: boolean;
}

const {
  title,
  description,
  ogTitle,
  ogDescription,
  ogImage,
  ogType = 'website',
  twitterCard,
  canonicalPath,
  currentPage = '',
  pageSchema,
  noindex = false,
} = Astro.props;

const siteUrl = 'https://vocabmine.com';
const canonicalUrl = canonicalPath ? `${siteUrl}${canonicalPath}` : undefined;
// Default OG / Twitter card image. Lives in /public so it's served at
// the site root and is the URL we reference in social meta tags.
// Pages override via the optional `ogImage` prop (e.g. blog posts pass
// the article's cover image).
const ogImageUrl = ogImage || `${siteUrl}/og-image.png`;

// Site-wide JSON-LD: WebSite (gets sitelinks search box if Google
// honours it) + Organization (knowledge-panel sidebar). Page-specific
// schema (SoftwareApplication, Article, FAQPage) is appended via the
// optional `pageSchema` prop so each page declares its own without
// duplicating the root graphs.
const siteSchemaGraph = [
  {
    '@type': 'WebSite',
    '@id': `${siteUrl}/#website`,
    url: siteUrl,
    name: SITE_NAME,
    description: SITE_DESCRIPTION,
    publisher: { '@id': `${siteUrl}/#organization` },
  },
  {
    '@type': 'Organization',
    '@id': `${siteUrl}/#organization`,
    name: SITE_NAME,
    url: siteUrl,
    logo: { '@type': 'ImageObject', url: `${siteUrl}/logo512.png` },
    email: SUPPORT_EMAIL,
  },
];
// `pageSchema` may be a single object or an array of objects (e.g. a
// page that wants both SoftwareApplication + FAQPage). Flatten so both
// shapes land at the top level of `@graph` instead of being nested.
const pageSchemaItems = Array.isArray(pageSchema) ? pageSchema : pageSchema ? [pageSchema] : [];
const schemaPayload = {
  '@context': 'https://schema.org',
  '@graph': [...siteSchemaGraph, ...pageSchemaItems],
};
const allowIndexing = import.meta.env.PUBLIC_ALLOW_INDEXING === 'true';
const gaMeasurementId = import.meta.env.PUBLIC_GA_MEASUREMENT_ID;

// Embed mode: when loaded inside a mobile app WebView (or iframe) via
// `?embed=app`, hide the marketing chrome (nav + footer) so the legal
// content reads cleanly without context-switching back to the site.
const embedMode = Astro.url.searchParams.get('embed') === 'app';
---

<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <script is:inline>
    // No-flash theme bootstrap. Runs synchronously before paint so the
    // [data-theme] attribute is already set when the body styles apply.
    // Light is the default brand look (matches the Vocab Mine 2 design);
    // we only switch to dark when the user has explicitly toggled it
    // (persisted in localStorage). The html tag ships with
    // data-theme="light" so the default holds even with JS disabled.
    (function () {
      try {
        var stored = localStorage.getItem('vocabmine-theme');
        document.documentElement.setAttribute('data-theme', stored || 'light');
      } catch (_) {
        /* localStorage blocked — the data-theme="light" on <html> stands. */
      }
    })();
  </script>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap"
    rel="stylesheet"
  />
  <title>{title}</title>
  {(noindex || !allowIndexing) && <meta name="robots" content="noindex, nofollow" />}
  {description && <meta name="description" content={description} />}
  {ogTitle && <meta property="og:title" content={ogTitle} />}
  {ogDescription && <meta property="og:description" content={ogDescription} />}
  <meta property="og:type" content={ogType} />
  <meta property="og:image" content={ogImageUrl} />
  <meta property="og:image:width" content="1200" />
  <meta property="og:image:height" content="630" />
  {canonicalUrl && <meta property="og:url" content={canonicalUrl} />}
  {twitterCard && <meta name="twitter:card" content={twitterCard} />}
  {twitterCard && ogTitle && <meta name="twitter:title" content={ogTitle} />}
  {twitterCard && ogDescription && <meta name="twitter:description" content={ogDescription} />}
  {twitterCard && <meta name="twitter:image" content={ogImageUrl} />}
  {canonicalUrl && <link rel="canonical" href={canonicalUrl} />}
  <link rel="icon" type="image/png" sizes="192x192" href="/logo192.png" />
  <link rel="apple-touch-icon" href="/logo192.png" />
  {/* JSON-LD structured data — emitted only when indexing is allowed
      so dev/staging environments don't pollute Google's understanding
      of the site with non-canonical hostnames. */}
  {allowIndexing && (
    <script is:inline type="application/ld+json" set:html={JSON.stringify(schemaPayload)} />
  )}
  {gaMeasurementId && (
    <>
      <script async src={`https://www.googletagmanager.com/gtag/js?id=${gaMeasurementId}`}></script>
      <script define:vars={{ gaMeasurementId }}>
        window.dataLayer = window.dataLayer || [];
        function gtag() { dataLayer.push(arguments); }
        gtag('js', new Date());
        gtag('config', gaMeasurementId, { anonymize_ip: true });
      </script>
    </>
  )}
</head>
<body class:list={[{ 'embed-app': embedMode }]}>
  {!embedMode && <Nav currentPage={currentPage} />}

  <main>
    <slot />
  </main>

  {!embedMode && <Footer />}

  <script>
    // --- Mobile Navigation Toggle ---
    const navToggle = document.getElementById('navToggle');
    const navLinks = document.getElementById('navLinks');

    if (navToggle && navLinks) {
      navToggle.addEventListener('click', function () {
        navToggle.classList.toggle('active');
        navLinks.classList.toggle('active');
      });

      navLinks.querySelectorAll('a').forEach(function (link) {
        link.addEventListener('click', function () {
          navToggle.classList.remove('active');
          navLinks.classList.remove('active');
        });
      });
    }

    // --- Smooth Scroll for Anchor Links ---
    document.querySelectorAll('a[href^="#"]').forEach(function (anchor) {
      anchor.addEventListener('click', function (e) {
        const targetId = this.getAttribute('href');
        if (targetId === '#') return;

        const target = document.querySelector(targetId!);
        if (target) {
          e.preventDefault();
          const navEl = document.querySelector('.nav');
          const navHeight = navEl ? navEl.offsetHeight : 0;
          const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - navHeight;
          window.scrollTo({
            top: targetPosition,
            behavior: 'smooth'
          });
        }
      });
    });

    // --- Scroll-triggered Fade-in Animations ---
    const fadeElements = document.querySelectorAll('.fade-in');

    if (fadeElements.length > 0 && 'IntersectionObserver' in window) {
      const observer = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            observer.unobserve(entry.target);
          }
        });
      }, {
        threshold: 0.15,
        rootMargin: '0px 0px -40px 0px'
      });

      fadeElements.forEach(function (el) {
        observer.observe(el);
      });
    } else {
      fadeElements.forEach(function (el) {
        el.classList.add('visible');
      });
    }

    // --- GA4 CTA Click Tracking ---
    if (typeof gtag === 'function') {
      document.querySelectorAll('[data-ga-cta]').forEach(function (el) {
        el.addEventListener('click', function () {
          gtag('event', 'cta_click', {
            cta_name: el.getAttribute('data-ga-cta'),
            cta_location: el.getAttribute('data-ga-location') || '',
            link_url: el.getAttribute('href') || '',
          });
        });
      });
    }
  </script>
</body>
</html>
